Taskino home hub BACKGROUND — single full frame (not a sprite sheet).

OUTPUT: ONE landscape scene, exact 16:9 widescreen, full-bleed edge to edge.
This image is ONE animation frame of a 4-frame loop. Generate only this frame.

COMPOSITION (locked across all 4 frames in the set):
- Centered modest stand patch for a tiny creature (packed earth ring / stone / element-appropriate surface).
- Stand surface sits at horizontal center; the pet stands on the CENTER of the disc (~84% down), not the back rim.
- Framing props allowed (trees, rocks, ruins) left/right — must not cover the stand.
- Deep midground + sky; readable silhouette hierarchy.

MOTION RULES FOR THIS FRAME (frame index: {FRAME}):
- Frame 0 = calm rest pose of the loop.
- Frame 1 = ambient motion begins (same scene, tiny change only).
- Frame 2 = peak of the same motion.
- Frame 3 = settle, almost back to frame 0.
- ONLY ambient particles / light / foliage tip may move.
- DO NOT move stand surface, horizon, camera, or major silhouettes.

STYLE:
- Clean detailed pixel-art RPG hub background (match creature-game pixel art, hard edges).
- No characters, creatures, eggs, UI, text, watermarks, grid lines, or frame numbers.
- No collage, no multi-panel, no 2×2 layout — single continuous image only.

ELEMENT THEME: {THEME}
ANIMATION STORY (same for all 4): {MOTION}
